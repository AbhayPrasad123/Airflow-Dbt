[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dag-generator"
version = "1.0.0"

[tool.setuptools]
packages = ["DagGenerator"]

[project.scripts]
generate-dag = "DagGenerator.cli:main"